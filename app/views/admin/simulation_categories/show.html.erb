<section id="show-simulation_category" class="col-12">
  <div class="row">
    <%= render :partial => 'layouts/notice', :locals => { :notice => notice, :alert => alert } %>
  </div>
  <div class="row">
    <div class="col-12">
      <div class="card">
        <h3 class="card-header"><%=@simulation_category.title %></h3>
        <div class="card-body">
          <dl>
            <dt><%= t(:simulations_count, scope: [:activerecord, :attributes, :simulation_category]) %></dt>
            <dd><%=  @simulation_category.simulations_count.to_s + '개' %></dd>
            <dt><%= t(:created_at, scope: [:activerecord, :attributes, :simulation_category]) %></dt>
            <dd><%= I18n.l @simulation_category.created_at.to_date, :format => :long %></dd>
            <dt><%= t(:updated_at, scope: [:activerecord, :attributes, :simulation_category]) %></dt>
            <dd><%= I18n.l @simulation_category.updated_at.to_date, :format => :long %></dd>
          </dl>
        </div>
      </div>
    </div>
  </div>
  <div class="row">
    <div class="col-12 col-xl-6">
      <%= link_to t(:link_list), admin_simulation_categories_path, class: 'btn btn-secondary' %>
    </div>
    <div class="col-12 col-xl-6 text-right">
      <%= link_to t(:edit), edit_admin_simulation_category_path(@simulation_category), class: 'btn btn-secondary' %>
      <%= link_to t(:delete), admin_simulation_category_path(@simulation_category), :method => :delete, :data => { :confirm => t(:delete_cascade_confirm) }, class: 'btn btn-danger' %>
    </div>
  </div>
</section>
