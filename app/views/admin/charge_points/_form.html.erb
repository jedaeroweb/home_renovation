<%= form_with(model: [:admin, charge_point], local: true) do |form| %>
  <% if charge_point.errors.any? %>
    <div id="error_explanation" class="alert alert-danger">
      <a href="#" class="close" data-dismiss="alert">&times;</a>
      <h2><%= pluralize(charge_point.errors.count, "개의 문제") %>로 진행할 수 없습니다.</h2>
      <ul>
        <% charge_point.errors.full_messages.each do |msg| %>
          <li>
            <%= msg %>
          </li>
        <% end %>
      </ul>
    </div>
  <% end %>
  <div class="card">
    <div class="card-body">
      <div class="form-group">
        <%= form.label :title %>
        <%= form.text_field :title, :class => "form-control", :required => "required" %>
      </div>
      <div class="form-group">
        <%= form.label :price %>
        <%= form.number_field :price, :class => "form-control"  %>
      </div>
      <div class="form-group">
        <%= form.label :point %>
        <%= form.number_field :point, :class => "form-control"  %>
      </div>
    </div>
  </div>
  <%= form.submit :class => 'btn btn-primary btn-block  btn-lg' %>
<% end %>