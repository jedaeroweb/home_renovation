<%
    a_class = 'nav-link'
    b_class = 'nav-link'
    c_class = 'nav-link'
    d_class = 'nav-link'

    if params[:controller] == 'admin/user_simulation_materials'
      a_class += ' active'
    end

    if params[:controller] == 'admin/simulations'
      b_class += ' active'
    end

    if params[:controller] == 'admin/simulation_materials'
      c_class += ' active'
    end

    if params[:controller] == 'admin/simulation_categories'
      d_class += ' active'
    end

  %>
  <div class="col-12">
    <nav class="sub-nav">
      <ul class="nav nav-pills">
        <li class="nav-item"><%= link_to t(:user_simulation_material, scope: [:activerecord, :models]), admin_user_simulation_materials_path, { :class => a_class } %></li>
        <li class="nav-item"><%= link_to t(:simulation, scope: [:activerecord, :models]), admin_simulations_path, { :class => b_class } %></li>
        <li class="nav-item"><%= link_to t(:simulation_material, scope: [:activerecord, :models]), admin_simulation_materials_path, { :class => c_class } %></li>
        <li class="nav-item"><%= link_to t(:simulation_category, scope: [:activerecord, :models]), admin_simulation_categories_path, { :class => d_class } %></li>
      </ul>
    </nav>
  </div>