<main>
  <section class="container">
    <div class="row">
      <h2 class="col-12 text-center"><%=t(:delete_user) %></h2>
      <div class="col-12">
        <p>회원탈퇴를 원하실 경우 아래의 버튼을 누르세요<br />
          기존 회원의 모든 정보가 제거되며 제거후에는 복구가 불가능합니다.
        </p>
        <%=form_tag user_registration_path, method: :delete do  %>
          <%=submit_tag t(:delete_user), :class=>"btn btn-block btn-lg btn-danger" %>
        <% end %>
      </div>
    </div>
  </section>
</main>